# ROADMAP — the living state (where are we)

> This file IS the program's memory. A fresh session re-reads it and knows exactly what to do next.
> Status: ✅ done · 🚧 in-progress · ⬜ todo. Flip to 🚧 when starting a module, ✅ when its Definition of Done is met. Add a **"touched"** note when a sprint changes an already-built module.
> Indexes: this file (work) + the [DATA_MODEL.md](./DATA_MODEL.md) Entity Catalog (data). Read those, not everything.

## At a glance
- **Modules:** 8 · **Sprints:** ~8 (0–7), dependency-ordered.
- **Current:** Sprint 6 **Player UI + dice + dashboard 🚧 in-progress**. Sprint 0–5 ✅ done. **Next: Sprint 7 — AI DM Assistant (after Sprint 6 done).**
- **Sprint 2 scope decisions (2026-06-19):** (a) **subclass coverage** = SRD 12 (1/class) **+ open community dataset** (OGL 1.0a / CC, e.g. open5e third-party) to add many more legally; copyrighted PHB-only subclasses (Assassin, Arcane Trickster, …) stay OUT — no legitimate open source. (b) **single sprint** (reference data + sheet together). Per-row `source` + license tracked; attribution shown.
- **Gates signed off:** Gate 0 (multi-module ✅) · Gate 1 (module split + ~8 sprints ✅) · Gate 2 (design tool = `ui-ux-pro-max`; tokens locked in [DESIGN_SYSTEM.md](./DESIGN_SYSTEM.md) ✅) · Gate 3 (stack: Next.js + TS, SQLite/Prisma, Socket.io, Ollama+Import-from-Claude ✅).

## Sprint × module status
| # | Sprint | Module(s) | Status | Definition of Done | Touched |
|---|--------|-----------|--------|--------------------|---------|
| 0 | Foundation | scaffold + lobby + roles + realtime + `LLMProvider` stub | ✅ done | DM creates a room, player joins by code, state syncs across clients | — |
| 1 | 5e Reference | spells / items / monsters (SRD 5.1 seed) | ✅ done | search and view statblocks & spells | — |
| 2 | Characters | 5e character sheet + class/subclass/race/background reference (SRD + open community) | ✅ done | player creates/edits their own character; picking class+subclass+race **auto-fills** defaults (hit dice→HP, prof bonus, saves, proficiency options, spell slots, features-by-level) | — |
| 3 | Inventory | equip + currency + attunement | ✅ done | items attach to a character; player manages own gear | — |
| 4 | Combat ⭐ | live initiative / HP / conditions tracker | ✅ done | run an encounter synced to the whole table | — |
| 5 | Story | sessions / quests / NPCs / journal | ✅ done | log a session and track plot threads | — |
| 6 | Player UI + dice + dashboard | player sheet view, dice broadcast, DM dashboard, search | 🚧 in-progress | a player can complete a full session end-to-end | — |
| 7 | AI DM Assistant | pluggable LLM (local default) + hybrid rules + Import-from-Claude | ⬜ todo | DM generates an NPC/loot/plot draft and approves it into the campaign | — |

## Why this order
Foundation first (everything needs realtime + auth + the LLM interface stub). Master/reference data (5e Reference) before the things that point at it. Characters before Inventory/Combat (both reference a character). Combat ⭐ once Characters + Monsters exist. Story after Combat (sessions log encounters). Player UI/Dice/Dashboard reads from everything, so it's late. AI last — it generates **into** the 5e/Character/Story entities, which must exist first.

> 5e makes schema heaviest at **Characters + Spells** (slots by level, conditions, proficiency, saves) — expect Sprints 1–2 to carry more schema than a typical app.

## Per-module docs
Produced just-in-time by `/kickoff` when each sprint starts, under `docs/modules/<module>/` (`PRD.md`, `SA_BLUEPRINT.md`, `UXUI_DESIGN.md`, …). Shared truth stays in `docs/program/`.

## Next action
Sprint 3 **Inventory ✅ DONE.** All 11 DoD items met. **209/209 tests pass** (20 files; +75 new: 25 rules + 25 service + 25 routes). `docs/modules/inventory/TEST_CASES.md` written (all PRD §5.1–5.16 edges + DoD 1–11 traced to evidence). **Next: Sprint 4 — Combat (`/roadmap` or `/kickoff` in fresh session).**
- Sprint 4 scope (plan): live initiative tracker + HP tracking + condition manager + turn order sync via Socket.io. Depends on Character (Sprint 2) + Monster reference (Sprint 1). See VISION.md §Sprint 4.
- Shipped earlier — Sprint 2 Characters: migration `characters` (8 tables + FK), seed (12 classes · 109 subclasses [12 SRD + 97 OGL community] · 936 features · 240 class-levels · 13 races · 42 backgrounds), `lib/characters/*`, `app/api/{reference,characters}/*`, `app/characters/*` (wizard + sheet + spell picker), nav link, [TEST_CASES.md](../modules/characters/TEST_CASES.md).
- **New GLOBAL reference entities this sprint** (additive migration `characters` — as-built one bundle; same pattern as `5e_reference`: no `campaignId`, `slug` key, seeded): `Class`, `Subclass`, `Race`, `Background`, `ClassLevel` (1–20 progression), `Feature`. Source = SRD 5.1 (5e-bits/5e-database) **+ open-licensed community packs** (OGL/CC) for extra subclasses; `source` + `license` per row.
- **`Character` entity finalized** (DATA_MODEL #character): references `Class`/`Subclass`/`Race`/`Background` by slug; **auto-fill** derives HP/prof-bonus/saves/skill-options/spell-slots/features from the reference data at pick time. `CharacterSpell` join finalized (owns). `PlayerSession.characterId` → real FK→Character via additive migration. References Sprint 1 `Spell` by slug — do NOT redesign reference tables.
- **Auto-fill = defaults, not a lock:** picking class+subclass+race pre-fills the sheet; player can still edit. Choices that 5e leaves to the player (skill picks, fighting style, ASI) are surfaced as options, not silently chosen.
- Sprint 1 shipped: migration `5e_reference` (3 GLOBAL tables), SRD seed (319/334/599), `lib/reference/*`, `app/api/reference/*`, `app/reference/*`, Reference nav link, [TEST_CASES.md](../modules/5e-reference/TEST_CASES.md). **76 unit tests pass**, typecheck/lint/build clean, live-verified.
- Run commands: `npm install` → `npm run db:migrate` → `npm run db:seed` → `npm run dev`. Tests: `npm test`.

## Next action
Sprint 5 Story **✅ DONE.** All 10 DoD items met. **501/501 tests pass** (26 files; +172 new: 70 rules + 69 service + 33 routes). `docs/modules/story/TEST_CASES.md` written (all PRD §5.1–5.20 edges + DoD 1–10 traced to evidence). **Next: Sprint 6 — Player UI + dice + dashboard (`/roadmap` or `/kickoff` in fresh session).**
- Sprint 6 scope (plan): player-facing sheet view, 3D dice broadcast via `@3d-dice/dice-box`, DM dashboard (session summary + quest overview), global search across 5e reference + characters + story. Depends on Characters (Sprint 2) + Combat (Sprint 4) + Story (Sprint 5).
- Shipped Stage 7 (`/dev`): migration `20260620200619_story` (CREATE Session/Quest/Npc/JournalEntry + indexes; **additive — verified no DROP/ALTER**). `lib/story/{types,rules,repo,service,http}.ts` (pure rules; tenant-scoped repo via `where:{campaignId}`; service re-derives authz via `resolveSession`→`canWrite`(=role==="dm"); markdown stored raw). REST `app/api/story/{sessions,quests,npcs,journal}` + `[id]` (8 files, force-dynamic, Next 16 `await params`). UI: `StorySection.tsx` (4-tab hub: Sessions/Quests/NPCs/Journal, DM CRUD + player read-only, modal forms, inline expand, objective checkboxes disabled for players, NPC alive/dead toggle, journal list+detail w/ Write/Preview) + safe `Markdown.tsx` (React elements only, no `dangerouslySetInnerHTML` — XSS-inert, edge 5.10) embedded below CombatTracker in `/campaign/[id]`. **329/329 prior tests green, lint clean, build clean.** Live-verified 18 edge checks vs real migrated DB (401/403/404/422, xp 0 ok / negative rejected, cross-campaign 404 + sessionId 422 [5.20], delete-session unlinks journal SET NULL [5.7], NPC alive toggle, quest status grouping).

## Session log

- **2026-06-21 (5)** — Sprint 6 dice polish (live-driven via real Chrome/puppeteer). Reworked the 3D dice so **the displayed number always matches the dice on the table**: discovered plain `@3d-dice/dice-box` cannot force predetermined faces (the `@N`/`value` notation is ignored — physics is the RNG; that feature lives in a separate package). Per user decision, **the client 3D dice are now the RNG** — client rolls real physics, reads the settled faces (`onRollComplete`), sends them as `clientRolls` to the server. Server **still validates** (new pure `rollFromFaces` in `lib/player-ui/dice.ts`: face count + range [1,sides] per mode, then recomputes the total server-side → anti-tamper) and persists/broadcasts; REST/tests fall back to server `Math.random` when `clientRolls` absent. Also fixed: result reveals only after dice settle (was instant); re-rolls work (tray no longer unmounts its canvas); multiple dice (2d6/4d8) verified; result shown in a centered tray modal (not fullscreen) with a `.dice-box-canvas{width/height:100%}` fix so dice fill the tray instead of a 300×150 corner. **+11 tests** (`tests/dice-rules.test.ts`) → **512/512 pass**, typecheck clean. **Backlog noted:** mixed-dice notation (e.g. `1d8+2d10+1d12` in one roll) deferred per user — roll each type separately for now; see memory `dice-mixed-notation-backlog`.
- **2026-06-21 (4)** — Sprint 5 Story Stage 8 (`/qa`, Sonnet 4.6). `tests/story-rules.test.ts` (70 tests — all pure functions in `lib/story/rules.ts`: validateXp, parseObjectives, validateObjectives, serializeObjectives, validateQuestStatus/isQuestStatus, validateSession/Quest/Npc/JournalEntry, summaryExcerpt, statusSortWeight; all LIMITS caps; PRD §5.1–5.4/5.11–5.13/5.18–5.19 covered). `tests/story-service.test.ts` (69 tests — full authz matrix DM/player/null ×4 entities; all §5 edges incl. 5.5/5.6 player→403, 5.14 campaignId from session not body, 5.15 cross-campaign→404, 5.16 no-session→401, 5.17 PATCH/DELETE non-existent→404, 5.20 cross-campaign sessionId→422). `tests/story-routes.test.ts` (33 tests — all 8 route files; mocked service + real http.ts; 401/403/404/422/200/201/204 per entity; edges 5.5/5.6/5.11–5.13/5.15–5.17/5.20). `docs/modules/story/TEST_CASES.md` (all §5.1–5.20 + DoD 1–10 traced to test evidence). **501/501 tests pass** (26 files; +172 new; prior 329 intact ✅). lint clean, build clean. **Sprint 5 Story ✅ DONE.**
- **2026-06-21 (3)** — Sprint 5 Story Stage 7 (`/dev`, additive — no scaffold; Opus 4.8). Migration `20260620200619_story` (CREATE TABLE Session/Quest/Npc/JournalEntry + 9 indexes; FK Campaign CASCADE, Npc.characterId→Character SET NULL, JournalEntry.sessionId→Session SET NULL; **verified additive — zero DROP/ALTER**). Back-relations added (no SQL change): `Campaign.{storySessions,quests,npcs,journalEntries}`, `Character.npcs`. `lib/story/types.ts` (views + lean list items + `StoryResult`), `rules.ts` (pure: validateXp/Objectives/QuestStatus/Session/Quest/Npc/JournalEntry, parseObjectives defensive, serializeObjectives, summaryExcerpt, statusSortWeight — no DB/LLM), `repo.ts` (Prisma CRUD ×4 entities, every query `where:{campaignId}`; `sessionExistsInCampaign` for cross-tenant guard), `service.ts` (orchestrate resolveSession→`canWrite`(role==="dm")→validate→repo→View; campaignId always from session, never payload), `http.ts` (respond/respondDelete/readJson helpers). REST: `app/api/story/{sessions,quests,npcs,journal}/route.ts` + `[id]/route.ts` (8 files; force-dynamic; Next 16 `await params`; 401/403/404/422/200/201/204). UI: `StorySection.tsx` (4-tab hub embedded below CombatTracker in `/campaign/[id]`; DM full CRUD + player read-only; modal forms Session/Quest/NPC w/ dynamic objective list; inline expand; quest objectives interactive for DM / `pointer-events:none` for player; status dropdown; NPC filter pills + faction filter + alive/dead skull toggle + dimmed dead cards; journal list+detail split w/ Write/Preview editor) + `Markdown.tsx` (SAFE renderer — React elements only, **no `dangerouslySetInnerHTML`**, raw HTML rendered inert → XSS edge 5.10). **329/329 prior tests green** (regression gate ✅), lint clean (exit 0), `npm run build` clean (all 8 routes registered ƒ dynamic). **Live-verified 18 checks vs real migrated dev.db**: 401 no-session, 403 player-write ×3 entities, 422 (negative xp / empty quest name / invalid status / empty NPC name / empty journal content / cross-campaign sessionId 5.20), xp 0 milestone allowed, player read OK, cross-campaign get → 404 (multi-tenancy), quest objectives + status grouping, NPC alive→dead toggle, **delete session → journal sessionId NULL (5.7, no cascade delete)**. **Sprint 5 still 🚧 — Stage 8 (`/qa`) remains: ~80 tests + TEST_CASES.md.**
- **2026-06-21 (2)** — Sprint 5 Story Stages 1–5 (`/kickoff` docs pipeline). Wrote `docs/modules/story/`: PRD (4 entities — Session/Quest/Npc/JournalEntry; 20 edge cases 5.1–5.20; DoD 10 items; REST-only, no Socket.io), SA_BLUEPRINT (Prisma schema for 4 new tables; migration `story` CREATE-only; `lib/story/{types,rules,repo,service}.ts` architecture; authz matrix DM/player/401/403/404/422; ~80 test plan), UXUI_DESIGN (4 sub-tabs: Sessions/Quests/NPCs/Journal; shared modal/toast/skeleton/delete-confirm patterns; responsive 1024/768/375; a11y notes), mockups/index.html (fully interactive — DM/Player toggle, all 4 tabs with pre-seeded data, expand/collapse cards, objective checkboxes, NPC alive/dead toggle, inline journal editing with markdown preview, modal forms for Session/Quest/NPC, delete confirm dialog, toast notifications). **Amended DATA_MODEL.md**: Session/Quest/Npc/JournalEntry finalized (replacing stubs); Location remains stub. **Flipped Sprint 5 → 🚧. Next: Stage 7 (`/dev`) in fresh session with Opus 4.8.**
- **2026-06-21 (1)** — Sprint 4 Combat Stage 8 (`/qa`). Wrote `tests/combat-rules.test.ts` (62 tests — all 10 pure functions in `lib/combat/rules.ts`: validateInitiative/HpDelta, applyDamage/Healing, parseConditions, applyCondition/removeCondition, shouldAutoUnconsciousOnKO, sortCombatants, advanceTurn, buildSnapshot). `tests/combat-service.test.ts` (50 tests — all 12 service functions: authz matrix, all PRD §5 edges). `tests/combat-routes.test.ts` (8 tests — 401/404/200 + multi-tenancy scope for both REST routes). `docs/modules/combat/TEST_CASES.md` (all 20 PRD §5 edges traced to test evidence + full DoD checklist). **329/329 tests pass** (23 files; +120 new). **Sprint 4 Combat ✅ DONE.** Next: Sprint 5 — Story.
- **2026-06-20 (4)** — Sprint 4 Combat Stage 7 (`/dev`). Migration `combat` (CREATE TABLE `Encounter` + CREATE TABLE `Combatant`; additive, verified no DROP/ALTER). `lib/combat/{types,rules,repo,service}.ts`. `server/combat.ts` (13 Socket.io intents: startEncounter→encounterStarted, endEncounter, addCombatant, removeCombatant, setInitiative, reorderTie, applyDamage, applyHealing, addCondition, removeCondition, nextTurn, setTurn, requestSnapshot). REST: `app/api/combat/active` + `app/api/combat/[encounterId]`. UI: `CombatTracker.tsx` embedded in `/campaign/[id]`. `app/providers.tsx` adds `getSocket()` to context. **209/209 prior tests pass** (regression gate ✅). Build clean.
- **2026-06-20 (3)** — Sprint 3 Inventory Stage 8 (`/qa`). Added `tests/inventory-routes.test.ts` (25 route-level smoke tests — 401/403/404/422/200/201 for all 5 endpoints; direct import of App Router handlers, mocked resolveSession + service layer). Wrote `docs/modules/inventory/TEST_CASES.md` (full PRD §5.1–5.16 × DoD 1–11 traceability with test evidence + live API log). **209/209 tests pass** (20 files). Flipped Sprint 3 → ✅ DONE.
- **2026-06-20 (2)** — Sprint 3 Inventory Stage 7 (`/dev`). Migration `inventory` (CREATE `CharacterItem` + ALTER `Character` ADD `currencyJson`; additive, data-preserving). `lib/inventory/{rules,types,repo,service}.ts`: pure rules (ATTUNEMENT_CAP=3, isAttunable, validateQuantity/Currency, normalizeCurrency, countAttuned); tenant-scoped repo (upsert-stack, updateItem, removeItem, getCurrencyRaw, setCurrency); service (addItem, setItemFields with attune `$transaction` for concurrency 5.11, removeItem, setCurrency, toInventoryView with missingRef/malformed-JSON graceful). REST: `app/api/characters/[id]/items` (GET/POST) + `[itemId]` (PATCH/DELETE) + `currency` (PATCH); all force-dynamic, 401/403/404/422. UI: `InventorySection`+`ItemRow`+`CurrencyStrip`+`AddItemModal` embedded in `CharactersClient.tsx` Sheet; inherits DESIGN_SYSTEM tokens. `tests/inventory-{rules,service}.test.ts` (25+25 tests). **184/184 tests pass**. Live-verified all PRD §5 edges (stacking, cap, not_attunable, attunement_limit, invalid_currency, unknown slug 404, delete+cascade). Build clean. **Sprint 3 still 🚧 — Stage 8 (`/qa`) + TEST_CASES.md remains.**
- **2026-06-20 (1)** — Sprint 3 Inventory `/roadmap` → `/kickoff` Stages 1→5 (docs). Flipped Sprint 3 → 🚧. Wrote `docs/modules/inventory/`: PRD (`CharacterItem` join + `Character.currency`; attunement cap 3 + requiresAttunement gate; carrying-capacity = non-goal/stretch; §5 edges 5.1–5.16; DoD 11 ข้อ incl. additive-migration regression gate), SA_BLUEPRINT (Prisma `CharacterItem` mirroring `CharacterSpell` — denormalized campaignId + **real relation `itemSlug`→`Item.slug`** + `@@unique([characterId,itemSlug])` stack-via-quantity; **`currencyJson` single JSON col** chosen over 5 Int cols; single additive migration `inventory` = CREATE CharacterItem + ALTER Character ADD currencyJson; `lib/inventory/{rules,types,repo,service}.ts`; **attunement cap enforced in `$transaction` reading live DB count** for concurrency 5.11; REST `app/api/characters/[id]/items(/[itemId])` + dedicated `/currency` route; authz matrix reusing `canWrite`/`resolveSession`; test plan), UXUI_DESIGN (Inventory section embedded in sheet — header+Attuned X/3 counter+CTA, item rows w/ equip Shield/ShieldCheck + attune Link2/Link **icon+label not color-only**, cap-disabled tooltip, add-item modal, currency strip + ≈gp total, state matrix, responsive accordion ≤768/375), mockups/index.html (clickable — toggle equip/attune respecting 3-cap, qty stepper, delete+confirm, add-item modal stack-on-dup, currency recompute+negative validation, empty state; **inline JS vm-parse-checked OK**). **Amended program DATA_MODEL:** `CharacterItem` finalized from stub; `currencyJson` recorded as cross-module additive column (Sprint 3). Stage 3 design = skipped (locked at program). **Sprint 3 still 🚧 — paused before build. Next: `/dev` (Stage 7, additive — no scaffold) + `/qa` (Stage 8).**
- **2026-06-19 (6)** — Sprint 2 Characters Stage 8 (`/qa`). Wrote [TEST_CASES.md](../modules/characters/TEST_CASES.md) (full PRD §5.1–5.15 + DoD 1–10 traceability). Closed coverage gaps with TDD — **+41 tests** (was 93 → **134**): `characters-transform` (11, SRD→schema mappers incl. computed prof bonus + community license), `characters-service` (16, derive vs PHB + override/recompute/resetAuto/HP-clamp + `canWrite` authz matrix + `manageSpell`), `characters-refrepo` (7, JSON parse + cache + malformed-fallback + null→404), `characters-auth` (7, bearer token + resolveSession). **Bug found+fixed:** picking a **subrace** directly (High Elf, Hill Dwarf) applied only the subrace delta, losing the base race's ability bonuses/speed/traits → `service.loadRefs` now resolves the parent race and merges (regression test added). **134 tests pass**, typecheck/lint/build clean, Foundation+Sprint 1 regression green. **Sprint 2 ✅ DONE.** Next: Sprint 3 — Inventory.
- **2026-06-19 (5)** — Sprint 2 Characters — **closed the two deferred DoD gaps before QA** (user: "ทำทุกอย่างให้จบก่อน qa"). (1) **Real community packs:** pulled OGL 1.0a content via Open5e API (verified per-document licenses), generated `community-subclasses.json` (**97 subclasses** across all 12 classes — Open5e Originals / Tome of Heroes (Kobold) / Tal'Dorei (Green Ronin)) + `community-backgrounds.json` (**41 backgrounds** + EN Publishing) → re-seeded: **109 subclasses · 42 backgrounds · 936 features**; rewrote `COMMUNITY-LICENSE.md` with OGL Section 15 chain. (2) **Spell picker (DoD #5 complete):** `CharacterSpell` ops in charRepo + `service.manageSpell` + `POST /api/characters/[id]/spells` (add/remove/prepare/unprepare, authz) + `toDetail` resolves real spell name/level; sheet Spells section (known list, prepare toggle, remove) + `SpellPicker` modal (class-filtered + search). **Live-verified:** Wizard L10 → slots 4/3/3/3/2 (PHB-correct for level) · added Fireball known→prepared · features compose class+subclass+background by level w/ descriptions. **93 tests pass**, typecheck/lint/build clean. **Sprint 2 🚧 — only Stage 8 (`/qa`) remains.** (Note: local dev.db has 1 orphan `oss-warlord` from the old sample seed → `npm run db:reset && db:seed` yields a clean 109.)
- **2026-06-19 (4)** — Sprint 2 Characters Stage 7 (`/dev`) **part 2 — seed + repos + REST + UI (Stage 7 COMPLETE)**. Vendored SRD (Classes/Subclasses/Features/Levels/Races/Subraces/Backgrounds) + community OGL subclass pack → `transform-characters.ts` + wired `prisma/seed/index.ts`; **seeded 12 classes · 13 subclasses (12 SRD + 1 community Warlord OGL) · 409 features · 240 class-levels · 13 races · 1 background**. ⚠️ **SRD reality: backgrounds = 1 (Acolyte only)** — rest are PHB-copyrighted (corrected PRD). Built `lib/characters/{types,glossary,refRepo,charRepo,service,auth}.ts` (reference cache, character repo tenant-scoped, derive/override/claim service, bearer-token authz). REST: `app/api/reference/{classes,races,backgrounds,subclasses}(/[slug])` + `app/api/characters(/[id])` CRUD w/ 401/403/404. UI: `app/characters/` (list + 7-step wizard w/ live PHB preview + point-buy/array + sheet w/ auto/override badges + Abilities/Skills/Subclass descriptions) + nav link. **Live-verified in preview:** created Dwarf Fighter (Champion) Lv3 via API → HP 31 / AC 11 / PB +2 / CON-save +5 / Athletics +4 all PHB-correct; claim (ownedByMe) works; sheet renders (design-system styled); level→4 PATCH recomputed HP 31→40; community subclass shows OGL license. **Bug fixed:** hard-refresh of /characters lost campaignId (no live state) → added stored-seat fallback. **93 tests pass**, typecheck/lint/build clean. **Sprint 2 still 🚧 — Stage 8 (`/qa`) remains.** Next: `/qa` (TEST_CASES + service/authz gap tests + regression).
- **2026-06-19 (3)** — Sprint 2 Characters Stage 7 (`/dev`) **part 1 of N — deterministic foundation** (additive, no scaffold). Added 8 Prisma models (6 GLOBAL reference + Character/CharacterSpell) + `PlayerSession.characterId` → @unique FK→Character; **single additive migration `20260619163354_characters`** (as-built; verified: CREATE 8 tables + data-preserving PlayerSession redefine; `foundation_baseline`/`5e_reference` untouched). Built `lib/characters/rules.ts` (pure auto-fill math — abilityMod/profBonus/maxHp/point-buy/standard-array/saves/skills/features) via **TDD: +17 tests vs PHB tables** + `lib/characters/glossary.ts` (ABILITY_INFO×6 + SKILL_INFO×18, SRD CC-BY). **93 tests pass** (was 76), typecheck clean. **Doc correction:** migration shipped as one `characters` (not 2) — back-relations couple the groups; updated SA §2.4 + DATA_MODEL markers. **CHECKPOINT — Sprint 2 still 🚧.** Resume Stage 7 part 2: `lib/characters/{types,refRepo,charRepo,service}.ts` → seed (vendor SRD Classes/Subclasses/Features/Levels/Races/Subraces/Backgrounds + community subclass pack → `transform-characters.ts` → wire `prisma/seed/index.ts` → run seed) → REST (`app/api/reference/{classes,races,backgrounds,subclasses}` + `app/api/characters` CRUD w/ authz) → UI (`app/characters` wizard+sheet) → build + preview verify → `/qa`.
- **2026-06-19 (2)** — Sprint 2 Characters `/kickoff` Stages 1→5 (docs). Wrote `docs/modules/characters/`: PRD (2 ชั้น GLOBAL ref + Character; auto-fill 13 ค่า; ability=array+point-buy; single-class 1–20; DoD 10 ข้อ + regression gate), SA_BLUEPRINT (8 Prisma models, 2 additive migration `characters_reference`+`characters`, single 1:1 claim relation `PlayerSession.characterId @unique` + denormalized `ownerSessionId` authz, soft-ref by slug, override model, rules engine spec, REST+authz 403/404, seed pipeline+license tracking), UXUI_DESIGN (wizard+preview, point-buy/array widgets, sheet, auto/override affordance — inherit tokens), mockups/index.html (clickable; both inline scripts node-parse-clean). **Amended program DATA_MODEL:** 8 entity → Finalized (Sprint 2); `PlayerSession.characterId` → @unique FK→Character (touched). Stage 3 design = skipped (locked at program). **Sprint 2 still 🚧 — paused before build (Stage 6+). Next: `/dev` (Stage 7, additive — no scaffold) + `/qa` (Stage 8).**
- **2026-06-19 (1)** — Sprint 2 Characters `/roadmap` kickoff. User asked for **comprehensive subclass defaults**. Surfaced SRD reality: SRD 5.1 has only **12 subclasses (1/class)**; non-SRD PHB subclasses are WotC-copyrighted. **Decisions:** (Q1) subclass = SRD + **open-licensed community dataset** (OGL/CC, e.g. open5e third-party) — NOT pirated PHB; (Q2) **single sprint** (reference data + sheet together). Adding 6 new GLOBAL reference entities (`Class`/`Subclass`/`Race`/`Background`/`ClassLevel`/`Feature`) + finalizing `Character`/`CharacterSpell` + `PlayerSession.characterId`→FK, all additive. Flipped Sprint 2 → 🚧. **Next: drive `/kickoff` characters, Stages 1→5 (docs).**
- **2026-06-16 (1)** — Program kickoff from `DND_CAMPAIGN_MANAGER.scope.md`. Gates 0–3 signed off. Wrote `docs/program/` (VISION, ARCHITECTURE, DATA_MODEL skeleton, DESIGN_SYSTEM, ROADMAP).
- **2026-06-16 (2)** — Sprint 0 Foundation `/kickoff`, Stages 1→5 (docs). Wrote `docs/modules/foundation/`: PRD, SA_BLUEPRINT (+ amended program DATA_MODEL: `Campaign`/`PlayerSession` finalized, `foundation_baseline` migration), UXUI_DESIGN, mockups/index.html. Stage 3 (design) skipped — locked at program level.
- **2026-06-16 (3)** — Sprint 0 Foundation Stages 6→7 (scaffold + dev). Commits `0969ec0` (scaffold) + `dba9f86` (features). Realtime lobby/roles/auth/persistence built; 21 unit tests + live DoD smoke all green (roster sync 14ms, restart rehydrates). **Sprint 0 ✅ done.**
- **2026-06-16 (4)** — Sprint 0 Foundation Stage 8 (`/qa`). Wrote `docs/modules/foundation/TEST_CASES.md` (full PRD §5/§7 traceability). Closed gaps: multi-tenancy isolation, invite-code collision retry, payload-size rejection, dup-name DB-race rollback, anon authz, reconnect token-index rebuild, no-LLM degrade. **35 unit tests pass** (was 21). Next: Sprint 1 — 5e Reference.
- **2026-06-17 (1)** — Sprint 1 5e Reference `/kickoff` Stages 1→5 (docs). Wrote `docs/modules/5e-reference/`: PRD, SA_BLUEPRINT (+ amended program DATA_MODEL: `Spell`/`Item`/`Monster` finalized, migration `5e_reference`, GLOBAL tables no `campaignId`, slug natural key), UXUI_DESIGN (statblock = hero screen), mockups/index.html (clickable prototype, client-side filter; node parse-check passed). Stage 3 (design) skipped — locked at program level. Sprint 1 still 🚧. **Next: scaffold/dev (Stage 6→7) + QA (Stage 8).**
- **2026-06-17 (3)** — Sprint 1 5e Reference Stage 8 (`/qa`). Wrote `docs/modules/5e-reference/TEST_CASES.md` (full PRD §5 + DoD traceability). Closed gaps with TDD: **+16 tests** — `reference-repo.test.ts` (JSON-column parse, lean-vs-detail shapes, null-on-missing→404 driver, malformed-JSON fallback, in-memory cache no-re-query; Prisma mocked via `vi.mock("@/lib/db")`) + `reference-gaps.test.ts` (empty-result paths, CR-max inclusive boundary, case-insensitive search, transform robustness for missing armor/profs/reactions). **Bug found+fixed:** statblock action ordering (Legendary before Reactions) → reordered to Actions→Reactions→Legendary, re-seeded. **76 tests pass** (was 60), typecheck/lint/build clean. **Sprint 1 ✅ DONE.** Next: Sprint 2 — Characters.
- **2026-06-17 (2)** — Sprint 1 5e Reference Stage 7 (`/dev`, additive — no scaffold). Migration `5e_reference` (3 GLOBAL tables, no campaignId, slug @unique, filterable scalars indexed). SRD 5.1 seed pipeline: vendored `5e-bits/5e-database` JSON → `prisma/seed/{transform,index}.ts`, idempotent upsert by slug, deterministic xp/crSort; **seeded 319 spells · 334 monsters · 599 items**; CC-BY-4.0 attribution at `prisma/seed/SRD-5.1-CC-BY-4.0.md`. `lib/reference/{types,parse,filter,srd,repo,http}.ts` (in-memory cache; dynamic routes — NOT force-static, since SQLite is seeded at runtime). REST `GET /api/reference/{spells|monsters|items}(/[slug])`. UI `app/reference/` (tabs + debounced search + client filter + list/detail, statblock hero, loading/empty/404, in-session guard, attribution footer) + Reference nav link in lobby. **TDD: +25 reference unit tests (60 total, all pass)**; typecheck clean; lint 0 errors; build clean; live-verified statblock/spell card/list-filter in preview (0 console errors). Sprint 1 DoD (search + view statblocks & spells) met. **Next: Stage 8 QA.** Known: `package.json#prisma` seed config deprecated in Prisma 7 (works on 6; migrate to `prisma.config.ts` at upgrade).
