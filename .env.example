# Copy to .env and adjust as needed.

# SQLite file database (local, single-host). Relative to /prisma.
DATABASE_URL="file:./dev.db"

# Port the single Node process (Next + Socket.io) listens on. Players join at http://<dm-ip>:PORT
PORT=3000
